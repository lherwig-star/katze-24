# Kopieren nach .env und ausfuellen. .env selbst NIE committen.

# Nur noetig, wenn ihr --via telegram nutzt (siehe src/notifier.py fuer die
# Einrichtung: @BotFather -> Token -> Bot in Gruppe einladen -> chat_id holen)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
